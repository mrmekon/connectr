sudo: required
language: rust
rust:
  - stable
  - beta
  - nightly
os:
 - osx
matrix:
  allow_failures:
    - rust: nightly
install:
- curl https://static.rust-lang.org/rustup.sh |
    sh -s -- --prefix=$HOME/rust
